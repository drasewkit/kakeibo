[mysqld]
# t4g.micro(1GiB)でMySQLが使いすぎないよう抑える。
# 既定のbuffer_pool 128MBに対し、周辺のバッファも含めた実使用量を下げる目的。
innodb_buffer_pool_size = 128M
innodb_log_buffer_size  = 8M
max_connections         = 30
performance_schema      = OFF
